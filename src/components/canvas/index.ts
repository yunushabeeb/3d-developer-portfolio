// Importing various canvas components for different 3D scenes

import EarthCanvas from './Earth'; // Import the EarthCanvas component, which renders the 3D Earth model
import BallCanvas from './BallCanvas'; // Import the BallCanvas component, which renders 3D balls with images
import ComputersCanvas from './ComputersCanvas'; // Import the ComputersCanvas component, which renders a 3D desktop PC
import StarsCanvas from './StarsCanvas'; // Import the StarsCanvas component, which renders a starry background

// Exporting the components to be used in other parts of the application
export { EarthCanvas, BallCanvas, ComputersCanvas, StarsCanvas };
